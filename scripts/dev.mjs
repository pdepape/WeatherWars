import{createServer}from'node:http';import{readFile,stat}from'node:fs/promises';import{extname,join}from'node:path';const types={'.html':'text/html','.css':'text/css','.js':'text/javascript','.webmanifest':'application/manifest+json','.png':'image/png'};createServer(async(req,res)=>{try{let p=join(process.cwd(),decodeURI(req.url.split('?')[0]));if((await stat(p)).isDirectory())p=join(p,'index.html');res.setHeader('content-type',types[extname(p)]||'application/octet-stream');res.end(await readFile(p))}catch{res.statusCode=404;res.end('Not found')}}).listen(4173,()=>console.log('Local URL: http://localhost:4173'));
