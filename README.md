# Weather Wars

A responsive, installable turn-based strategy game where terrain, infrastructure, and weather powers determine the winner.

Run `npm run dev` for local development and `npm run build` to produce the deployable site.
